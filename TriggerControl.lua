rednet.wrap("top")

while true do
    local message = ""
    local senderId, message, protocol = rednet.receive("state", 10)
    if message then 
        #something to turn the farm off, 

    else: 

        #normal cycale 
    
end